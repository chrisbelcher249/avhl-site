// Legacy plural endpoint retained for backward compatibility.
// The canonical lineup endpoint is /api/lineup/[abbreviation].
export { runtime, dynamic, revalidate, GET, POST } from "../../lineup/[abbreviation]/route";
